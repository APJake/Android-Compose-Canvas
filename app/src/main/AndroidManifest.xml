<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.apjake.compose_canvas">

  <application
      android:allowBackup="true"
      android:dataExtractionRules="@xml/data_extraction_rules"
      android:fullBackupContent="@xml/backup_rules"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.ComposeCanvas"
      tools:targetApi="31">
    <activity
        android:name=".features.basic_canvas.BasicCanvasActivity"
        android:exported="false"
        android:label="@string/title_activity_draw_text"
        android:theme="@style/Theme.ComposeCanvas" />
    <activity
        android:name=".features.star_clicker.StarClickerActivity"
        android:exported="false"
        android:label="@string/title_activity_star_clicker"
        android:theme="@style/Theme.ComposeCanvas" />
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:theme="@style/Theme.ComposeCanvas">
    </activity>
    <activity
        android:name=".features.weight_picker.WeightPickerActivity"
        android:exported="true"
        android:theme="@style/Theme.ComposeCanvas">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>